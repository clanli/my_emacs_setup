# my_emacs_setup

```bash
$ pip install jedi autopep8 flake8 ipython importmagic yapf
$ sudo apt install emacs
$ git clone https://github.com/clanli/my_emacs_setup.git
$ cp my_emacs_setup/init.el ~/.emacs.d/init.el
```
